OPENAI_API_KEY=
ANTHROPIC_API_KEY=
GOOGLE_API_KEY=

YOLO_SERVICE_URL=http://localhost:8080
IMG_PROC_MCP_URL=http://localhost:9000/mcp

MODEL=anthropic:claude-haiku-3-5

AWS_REGION=us-east-1
AWS_S3_BUCKET=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# ALLOWED_ORIGINS (CORS) is NOT read from this file - docker-compose.yaml
# sets it directly via ${ALLOWED_ORIGINS:-http://localhost:3000}, exported
# by the CI deploy step's shell for each deploy. Not needed here.
